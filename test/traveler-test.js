import chai from 'chai';
const expect = chai.expect;
import { Traveler } from "/src/Traveler.js";

describe('See if Traveler tests are running', function() {

  

  it('should return true', function() {
    expect(true).to.equal(true);
  });
});
